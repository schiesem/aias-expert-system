expert_system/
│── modules/         # Hauptpaket-Verzeichnis
│   │── __init__.py     # Initialisiert das Paket
│   │── module1.py      # Ein Modul
│   │── module2.py      # Ein weiteres Modul
│── tests/              # Tests für dein Paket
│   │── test1.py
│── setup.py            # Paket-Setup für Installation
│── pyproject.toml      # Modernes Build-System (optional)
│── README.md           # Beschreibung des Pakets
│── LICENSE             # Lizenzdatei
│── .gitignore          # Git-Ausschlussdatei (optional)

Die Datei reasonning.py ist eine angepasste Komponente der reasonning.py aus der owlready2 bibliothek.